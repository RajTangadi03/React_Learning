var a = 10;

// Two types of exports in ES6 modules - named export and default export
// Named export - export {a, b, c};
// Default export - export default a;

export default a; // if we want to export a single value from a whole file then use default keyword
// export {a}; // this is named export